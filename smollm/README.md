# SmolLM

This module contains the native code for running the LLaMA models.

## Building with Address Sanitizer

To get a more detailed backtrace in case of a crash, please build the app with Address Sanitizer enabled.
I have already enabled it in the `build.gradle.kts` file.

Please rebuild the app and provide the new crash log if the app still crashes.
